# Task-specific configurations for GUE (Genome Understanding Evaluation)
# 28 datasets across 7 task categories

# ========== Promoter Prediction Tasks ==========
# Human promoter detection
human_promoter:
  train_loader:
    max_seq_len: 512
  eval_loader:
    max_seq_len: 512

# Mouse promoter detection
mouse_promoter:
  train_loader:
    max_seq_len: 512
  eval_loader:
    max_seq_len: 512

# ========== Promoter Tasks ==========
prom_300_all:
  train_loader:
    max_seq_len: 300
  eval_loader:
    max_seq_len: 300

prom_300_notata:
  train_loader:
    max_seq_len: 300
  eval_loader:
    max_seq_len: 300

prom_300_tata:
  train_loader:
    max_seq_len: 300
  eval_loader:
    max_seq_len: 300

prom_core_all:
  train_loader:
    max_seq_len: 300
  eval_loader:
    max_seq_len: 300

prom_core_notata:
  train_loader:
    max_seq_len: 300
  eval_loader:
    max_seq_len: 300

prom_core_tata:
  train_loader:
    max_seq_len: 300
  eval_loader:
    max_seq_len: 300

# ========== Splice Site Tasks ==========
splice_reconstructed:
  train_loader:
    max_seq_len: 600
  eval_loader:
    max_seq_len: 600

# ========== Virus Tasks ==========
virus_covid:
  train_loader:
    max_seq_len: 1024
  eval_loader:
    max_seq_len: 1024

# ========== Epigenetic Marks Prediction (EMP) ==========
# Human histone marks
human_h3:
  train_loader:
    max_seq_len: 600
  eval_loader:
    max_seq_len: 600

human_h3k14ac:
  train_loader:
    max_seq_len: 600
  eval_loader:
    max_seq_len: 600

human_h3k36me3:
  train_loader:
    max_seq_len: 600
  eval_loader:
    max_seq_len: 600

human_h3k4me1:
  train_loader:
    max_seq_len: 600
  eval_loader:
    max_seq_len: 600

human_h3k4me2:
  train_loader:
    max_seq_len: 600
  eval_loader:
    max_seq_len: 600

human_h3k4me3:
  train_loader:
    max_seq_len: 600
  eval_loader:
    max_seq_len: 600

human_h3k79me3:
  train_loader:
    max_seq_len: 600
  eval_loader:
    max_seq_len: 600

human_h3k9ac:
  train_loader:
    max_seq_len: 600
  eval_loader:
    max_seq_len: 600

human_h4:
  train_loader:
    max_seq_len: 600
  eval_loader:
    max_seq_len: 600

human_h4ac:
  train_loader:
    max_seq_len: 600
  eval_loader:
    max_seq_len: 600

# ========== Transcription Factor Binding Sites ==========
tf_0:
  train_loader:
    max_seq_len: 512
  eval_loader:
    max_seq_len: 512

tf_1:
  train_loader:
    max_seq_len: 512
  eval_loader:
    max_seq_len: 512

tf_2:
  train_loader:
    max_seq_len: 512
  eval_loader:
    max_seq_len: 512

tf_3:
  train_loader:
    max_seq_len: 512
  eval_loader:
    max_seq_len: 512

tf_4:
  train_loader:
    max_seq_len: 512
  eval_loader:
    max_seq_len: 512

# ========== Mouse Tasks ==========
mouse_0:
  train_loader:
    max_seq_len: 512
  eval_loader:
    max_seq_len: 512

mouse_1:
  train_loader:
    max_seq_len: 512
  eval_loader:
    max_seq_len: 512

mouse_2:
  train_loader:
    max_seq_len: 512
  eval_loader:
    max_seq_len: 512

mouse_3:
  train_loader:
    max_seq_len: 512
  eval_loader:
    max_seq_len: 512

mouse_4:
  train_loader:
    max_seq_len: 512
  eval_loader:
    max_seq_len: 512